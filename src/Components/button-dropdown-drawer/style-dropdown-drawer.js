import styled from 'styled-components';
import MuiAccordion from '@material-ui/core/Accordion';

const muiAccordion = styled(MuiAccordion)`
  color: #007c4b;
  font-weight: 700;
  font-family: 'Poppins', sans-serif;
`;

export default muiAccordion;
